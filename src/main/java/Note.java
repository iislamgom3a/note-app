import java.util.List;

public class Note {
    private int noteID;
    private String title;
    public Note(){
        // at a GUI component to show the title of the note separately
    }
    public void addImage(){
        // Using drag and drop form the OS
        // take a copy of the image and put it at the object folder
        // add the path of it at the .md file
        // it .md reader will automatically read it as an image
    };
    public void addSketch(){
        // compose a sketch
        // call addImage
        // the added sketch isn't editable
    };
}
